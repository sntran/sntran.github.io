name: Trần Nguyễn Sơn's Space
encoding: "utf-8"

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "index.html"
